SELECT * FROM dream_shops_db.category;

-- -----------------------------------------------------------
create database company;
use company;
show tables;
desc emdb;
select * from emp;

create database if not exists company;

create database college;
-----------------------------------------------------------
create database School;
use school;
create table course(
cid int,
cname varchar(50)
);

show tables;

insert into course (cid, cname) values (1, "phy"), (2, "maths");

select * from course;
select cid from course;
select cname from course;

-- unique constraint----------------------------------------------------------------------------------------

create table student(phoneno int unique);
insert into student(phoneno) values (123),(325),(1238);
-- not-null constraint----------------------------------------------------------------------------------------


-- check constraint----------------------------------------------------------------------------------------

create table student1(
age int check(age>18)
);
insert into student1(age) values (11);

-- primary key----------------------------------------------------------------------------------------
create table student2(age int, rollno int primary key);
-- --------unique-----------
insert into student2(age, rollno) values(12,1),(13,2),(14,3);

-- ------------foreign key-----------
create table student3(cname varchar(50), rollno int, foreign key (rollno) references student2(rollno));
-- -------------------------------------
CREATE TABLE STUD(
ID INT PRIMARY KEY,
NAME VARCHAR(50)
);

CREATE TABLE COURSE1(
CID INT PRIMARY KEY,
ID INT, FOREIGN KEY (ID) REFERENCES STUD(ID)
);
-- -------------------------------------
CREATE DATABASE XYZ;
USE XYZ;

CREATE TABLE EMPLOYEE(
ID INT PRIMARY KEY,
NAME VARCHAR(50),
AGE INT,
DEPARTMENT VARCHAR(50),
CITY VARCHAR(50),
SALARY INT);

INSERT INTO EMPLOYEE(ID, NAME, AGE, DEPARTMENT, CITY, SALARY) VALUES 
(1,"RAHUL", 25, "IT", "MMUMBAI", 1500),(2,"AFSARA",26, "HR", "PUNE",2000),
(3, "ABHIMANYU",27, "IT", "MUMBAI", 25),(4,"ADITYA", 25, "MARKETING", "SURAT",2400)
,(5,"RAJ",24,"FINANCE", "INDORE",1500);

SHOW TABLES;
DESC EMPLOYEE;
SELECT * FROM EMPLOYEE;
SELECT AGE,NAME,CITY FROM EMPLOYEE;

-- -------------------UPDATE TABLE
UPDATE EMPLOYEE 
SET SALARY = 50000
WHERE DEPARTMENT = "HR";

SET SQL_SAFE_UPDATES=0;

UPDATE EMPLOYEE 
SET NAME="RAJ"
WHERE NAME = "RAAJ";

UPDATE EMPLOYEE 
SET SALARY=2500
WHERE SALARY = 25;

UPDATE EMPLOYEE 
SET CITY='MUMBAI'
WHERE CITY = 'MMUMBAI';

DELETE FROM EMPLOYEE WHERE DEPARTMENT = "hr";
SELECT * FROM EMPLOYEE;
SELECT * FROM EMPLOYEE WHERE AGE > 22;


-- ---------------ALTER TABLE ADD ATTRIBUTE COLUMN-----------------
ALTER TABLE EMPLOYEE 
ADD DOB VARCHAR(20) DEFAULT "NP";
DESC EMPLOYEE;

-- ---------------ALTER TABLE DROP ATTRIBUTE COLUMN-----------------
ALTER TABLE EMPLOYEE 
DROP COLUMN DOB;

-- ---------------MODIFY  DATA TYPE TABLE COLUMN-----------------
ALTER TABLE EMPLOYEE 
MODIFY AGE VARCHAR(10);

-- ---------------CHANGE NAME COLUMN-----------------
ALTER TABLE EMPLOYEE 
CHANGE AGE EMP_AGE VARCHAR(9);
DESC EMPLOYEE;

-- ---------------RENAME COLUMN NAME -----------------
ALTER TABLE EMPLOYEE
RENAME COLUMN EMP_AGE TO AGE ;

-- ---------------RENAME TABLE NAME -----------------
RENAME TABLE EMPLOYEE TO EMPLOYEES;
DESC EMPLOYEES;

RENAME TABLE EMPLOYEES TO EMPLOYEE;
-- ---------------RENAME COLUMN NAME -----------------
ALTER TABLE EMPLOYEE 
RENAME COLUMN NAME  TO EMP_NAME;
DESC EMPLOYEE;

-- ---------------TRUNCATE COMMAND- PRESERVED SRUCTURE OF TABLE -----------------
CREATE TABLE SALARYDETAILS (
SALARY INT, INCREMENT INT
);

DESC SALARYDETAILS;

INSERT INTO SALARYDETAILS(SALARY, INCREMENT) 
VALUES(1200,6),(1500,4);

SELECT * FROM SALARYDETAILS; 

-- ---------------TRUNCATE COMMAND- PRESERVED SRUCTURE OF TABLE  ONLY DATA GONE-----------------
TRUNCATE table SALARYDETAILS ;

-- ---------------DISTINCT QUERY-----------------
DROP TABLE EMPLOYEE;

SELECT * FROM EMPLOYEE;

SELECT DISTINCT DEPARTMENT FROM EMPLOYEE;

SELECT DISTINCT DEPARTMENT, AGE FROM EMPLOYEE;


-- ------------------OPERATORS-----------------------
-- ---AND
SELECT * FROM EMPLOYEE WHERE CITY = 'PUNE' AND AGE > 18;

-- -----OR
SELECT * FROM EMPLOYEE WHERE CITY = 'PUNE' OR AGE > 18;

-- ----NOT
SELECT * FROM EMPLOYEE WHERE DEPARTMENT NOT IN ('IT','HR');

-- ----IN ----- 
SELECT * FROM EMPLOYEE WHERE DEPARTMENT IN('IT','HR');

-- ----NOT NULL ----- 
SELECT * FROM EMPLOYEE WHERE DEPARTMENT IS NOT NULL;

-- ----LIKE WILDCARDS OPERATORS----- 
-- --MACHES SET OF CHARACHTERS
SELECT * FROM EMPLOYEE WHERE NAME LIKE 'A%';

-- ----MATCHES ANY SINGLE CHARACTERS----- 
SELECT * FROM EMPLOYEE WHERE NAME LIKE '_A%';

-- ----BETWEEN ----- 
SELECT * FROM EMPLOYEE WHERE SALARY between 1200 AND 2000;


-- ==========----CLAUSES ============----- 
-- WHERE, GROUP BY , HAVING , ORDER BY, LIMIT 

-- WHERE
SELECT * FROM EMPLOYEE WHERE AGE  > 25;

-- LIMIT-- RESTRICT THE NUMER OF ROWS RETURNED BY THE QUERY.
SELECT * FROM EMPLOYEE LIMIT 2;

-- ORDER BY -- EITHER ASCENDING OR DESCENDING ORDER.
SELECT * FROM EMPLOYEE ORDER BY SALARY DESC; -- SALARY HIGEST TO LOWEST
SELECT * FROM EMPLOYEE ORDER BY SALARY; -- BY DEFAULT ASCENDING 

-- --------======-----*******--QUESTIONS--******---==========------------------------------

-- WRITE A QUERY TO FETCH THE DETAILS OF EMPLOYEES HAVING SALARY GREATER THAN 1200 AND CITY AS MUMBAI.
SELECT * FROM EMPLOYEE 
WHERE SALARY > 1200  AND CITY =  'MUMBAI';



-- ==========----AGGREGATE FUNCTION USE FOR CALCULATIOINS============----- 
-- COUNT(), SUM(), AVG(), MIN(), MAX(), GROUP CONCAT().

-- COUNT()
SELECT COUNT(NAME) FROM EMPLOYEE;

-- SUM()
SELECT SUM(SALARY) FROM EMPLOYEE;

-- AVG()
SELECT AVG(SALARY) FROM EMPLOYEE;

-- MIN() MAX()
SELECT MIN(SALARY) FROM EMPLOYEE;
SELECT maX(SALARY) FROM EMPLOYEE;

-- GROUP BY CLAUSE-- THIS IS USED TO GROUP ROWS THAT HAVE THE SAME VALUES INTO TOGETHER 
-- IT HELPS TO ORGANISE DATA INTO GROUPS SO THAT YOU CAN DO CALCULATIONS 
-- LIKE FINDING TOTALS OR AVARAGES,  FOR EACH GROUP

SELECT DEPARTMENT, AVG(SALARY) AS AVGSAL 
FROM EMPLOYEE 
GROUP BY DEPARTMENT;


-- HAVING CLAUSE
SELECT DEPARTMENT, AVG(SALARY) AS AVGSAL 
FROM EMPLOYEE 
GROUP BY DEPARTMENT
HAVING AVGSAL > 1500;

SELECT DEPARTMENT, AVG(SALARY) AS AVGSAL 
FROM EMPLOYEE 
GROUP BY DEPARTMENT
HAVING AVGSAL >= 1500;

-- QUESTION--
-- WRITE A QUERY TO FIND THE TOTAL NUMBER OF EMPLOYEES IN EACH CITY.

SELECT CITY, COUNT(NAME) AS NO_OF_EMP
FROM EMPLOYEE 
GROUP BY CITY;

-- QUESTION--
-- WRITE A QUERY TO FIND THE MAXIMUM SALARY OF EMPLOYEES IN EACH CITY IN DESCENDING ORDER.

SELECT CITY, MAX(SALARY) AS MAX_SALARY
FROM EMPLOYEE
GROUP BY CITY 
ORDER BY MAX_SALARY DESC;

 -- QUESTION--
-- WRITE A QUERY TO DISPLAY THE DEPARTMENT NAMES ALONGSIDE THE TOTAL COUNT OF EMPLOYEES
-- IN EACH DEPARTMENT, SORTNING THE RESULTS BY THE TOTAL NUMBER OF EMPLOYEES
-- IN DESCENDING ORDER.

SELECT DEPARTMENT, COUNT(ID) AS TOTAL_EMP
FROM EMPLOYEE 
GROUP BY DEPARTMENT
ORDER BY TOTAL_EMP DESC;

 -- QUESTION--
 -- WRITE A QUERY TO LIST THE DEPARTMENT WHERE THE AVERAGE SALARY
 -- IS GREATER THAN 1200, ALSO DISPLAY THE DEPARTMENT AND THE AVERAGE SALARY.

SELECT DEPARTMENT, AVG(SALARY) AS AVG_SALARY
FROM EMPLOYEE 
GROUP BY DEPARTMENT
HAVING AVG(SALARY) > 2000;

-- ****** jOINING TABLE ******----
-- INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL JOIN, SELF JOIN, CROSS JOIN
-- INNER JOIN -----
CREATE TABLE CUSTOMER (
ID INT,
NAME VARCHAR(50)
);

INSERT INTO CUSTOMER(ID,NAME) VALUES(1,"RAHUL"),
(2,"AFSARA"),(3,"ABHIMANYU"),(4,"ADITYA"),(5,"RAJ");

SELECT * FROM CUSTOMER;

CREATE TABLE ORDERS (
ID INT PRIMARY KEY,
ORDERNAME VARCHAR(50)
);

INSERT INTO ORDERS(ID, ORDERNAME)
VALUES(2,"FRUIT"),(3,"BALL"),(4,"UTENSIL");

SELECT * FROM ORDERS;

SELECT * FROM CUSTOMER
INNER JOIN ORDERS
ON CUSTOMER.ID = ORDERS.ID;

-- LEFT INNER JOIN -----

SELECT * FROM CUSTOMER
LEFT JOIN ORDERS
ON CUSTOMER.ID = ORDERS.ID;

-- RIGHT INNER JOIN -----

SELECT * FROM CUSTOMER
RIGHT JOIN ORDERS
ON CUSTOMER.ID = ORDERS.ID;

-- FULL OUTER JOIN -----
SELECT * FROM CUSTOMER
LEFT JOIN ORDERS
ON CUSTOMER.ID = ORDERS.ID
UNION
SELECT * FROM CUSTOMER
RIGHT JOIN ORDERS
ON CUSTOMER.ID = ORDERS.ID;

-- CROSS JOIN -----
SELECT * FROM CUSTOMER 
CROSS JOIN ORDERS;

-- SELF JOIN -----
create TABLE STUDENT(
S_ID INT,
NAME VARCHAR(50),
MENTOR_ID INT
);

INSERT INTO STUDENT(S_ID, NAME, MENTOR_ID) 
VALUES (1, 'RAM', NULL),
(2, 'RAHUL', 1),(
3, 'RITI', 1),
(4,'RIYA',3);

SELECT * FROM STUDENT;

SELECT T1.NAME AS MENTOR_NAME, T2.NAME AS STUDENT_NAME 
FROM STUDENT AS T1
JOIN STUDENT AS T2
WHERE T1.S_ID = T2.MENTOR_ID;

-- ------------------------------------------------------------------------------

-- -----------------AggregateD Function----------------------------------------------
-- AVG() 

USE  XYZ;
SHOW TABLES;
SELECT * FROM EMPLOYEE;

SELECT SUM(SALARY) FROM EMPLOYEE;
SELECT COUNT(NAME) FROM EMPLOYEE;
SELECT AVG(SALARY) FROM EMPLOYEE;
SELECT MIN(SALARY) FROM EMPLOYEE; -- MIN VALUE IN COLUMN
SELECT MAX(SALARY) FROM EMPLOYEE;

-- --------QUESTION== TO FIND ALL THE EMPLOYEES HAVING SALARY GREATER THAN MIN SALARY
SELECT NAME, SALARY
FROM EMPLOYEE
WHERE SALARY > (SELECT MIN(SALARY) FROM EMPLOYEE);

-- SQL SUBQUERIES/ NESTED QUERIES
-- SUBQUERIES IN WHERE
-- TO FIND MINIMUM AGE

-- MIN AGE -- REPLACING ALTERNATIVE  NAME
SELECT MIN(AGE) AS MIN_AGE 
FROM EMPLOYEE; 

-- ALL THE EMPLOYEES WHO HAVE AGE > MIN_AGE
SELECT EMP.NAME, EMP.AGE
FROM EMPLOYEE EMP, 
(SELECT MIN(AGE) AS MIN_AGE 
FROM EMPLOYEE) AS MIN_AGE_QUERY
WHERE EMP.AGE > MIN_AGE_QUERY.MIN_AGE;


-- PRINT THE EMPLOYEES WITH THE AVERAGE AGE AND AGE OF EMPLOYEES
-- AVERAGE AGE
-- PRINT THE EMPLOYEES AGE ANE AVG_AGE
SELECT (SELECT AVG(AGE) FROM EMPLOYEE) AS AVG_AGE, AGE 
FROM EMPLOYEE;

-- ----------FIND N-TH HIGHEST SALARY------------------
-- 4TH HIGHEST SALARY
SELECT DISTINCT SALARY 
FROM EMPLOYEE 
ORDER BY SALARY DESC;

SELECT * FROM EMPLOYEE;

SELECT DISTINCT SALARY 
FROM EMPLOYEE 
ORDER BY SALARY DESC 
LIMIT 3,1;	-- 3,1 IS M,N 
-- M == NUM OF ROWS WHICH NEED TO BE SKIP
-- N -= NUM OF ROWS WHICH WE WANT TO SHOW AFTER BEING SKIP

-- ============ STORED PROCEDUTE IN SQL===========================
-- THESE ARE PROGRAMES THAT CAN PERFORM TASKS BASED ON THE STORED QUERY 
-- IT IS BASICALLY A COLLECTION OF PRE-WRITTEN SQL STATEMENTS 
-- GROUPED TOGETHER UNDER A SPECIFIC NAME. 

-- IT IS TWO TYPES 1. PARAMETER 2. EMPTY

-- QUERY : (TO CREATE A PROCEDURE)
-- CREATE PROCEDURE PROCEDURENAME() 
-- BEGIN 
-- QUERY
-- END;

-- QUERY : (TO CALL A PROCEDURE)
-- CALL PROCEDURE NAME();

-- CREATE PROCEDURE WITHOUT PARAMS 
-- QUERY 1
 
DELIMITER /
CREATE PROCEDURE GETORDERDETAILS() 
BEGIN
SELECT * FROM ORDERS ;
END /
DELIMITER ;

CALL GETORDERDETAILS();
-- ----------------------------------
-- STORE PROCEDURE WITH PARAMS


-- --------------------------------

-- VIEWS IN SQL
-- A VIEW IS A VIRTUAL TABLE IN SQL IT HELPS IN PROVIDING  A FILTERED VIEW 
-- OF DATA FOR SEC PURPOSE.
-- -------------
CREATE VIEW EMPLOYEEVIEW AS
SELECT ID, NAME, CITY FROM EMPLOYEE;

SELECT ID FROM EMPLOYEEVIEW; -- IT ONLY SHOWS VALID DETAILS AND HIDE SECURE DATA
-- -------------

-- CASE AND IF IN SQL
-- CASE -- IT ALLOWS YOU TO PERFORM CONDITIONAL LOGIC WITHIN A QUERY. IT CAN BE USED IN
-- BOTH SELECT AND UPDATE STATEMENTS TO EVALUATE CONDITIONS AND RETURN SPECIFIC VALUES BSED ON THOSE 
-- CONDITIONS.

-- --================================================================

-- Q. WRITE THE SQL QUERY TO 1. CREATE A DATABASE COMPANY 
-- 2. CREATE A EMPLOYEE IN IT DELETE/DROP HE DATABSE.
-- DROP
-- DROP DATABSE COMPANY;
-- ----------------------------
 -- Q. WRITE THE SQL QUERY TO 1. CREATE TABLE EMPLOYEE 2. INSERT DATA INTO THE TABLE EMPLOYEE
 -- 3. UPDATE SALARY FOR ALL PEOPLE IN HR DEPARTMENT TO 20000 4. DELETE DATA FOR EMPLOYEE HAVING 
 -- EMPID=1, 5. DELETE ENTIRE TABLE.
 -- 1. CREATE TABLE EMPLOYEE
 
 CREATE TABLE EMPLOYEE1 (
 EMPID INT PRIMARY KEY,
 NAME VARCHAR(50),
 DEPARTMENT VARCHAR(50),
 SALARY INT
 );
 DESC EMPLOYEE1;
 SHOW TABLES;
 -- 2. INSERT DATA INTO EMLOYEE
 INSERT INTO EMPLOYEE1(EMPID, NAME, DEPARTMENT, SALARY)
 VALUES (4,'SIDHART', 'HR', 18000);
 
 SELECT * FROM EMPLOYEE1;

 -- 3 UPDATE ALL HR DEPARTMENT SALARY 
 -- FOR ALL
UPDATE EMPLOYEE1 
SET SALARY = 50000
WHERE DEPARTMENT = "HR";

-- ACTIVATING SAFE MODE ---	
SET SQL_SAFE_UPDATES=0;
 
 -- INDIVIDUAL UPADATE----
-- UPDATE EMPLOYEE1 
-- SET SALARY=25000
-- WHERE SALARY = 25;

-- ONLY DATA DELETION
DELETE FROM EMPLOYEE1;
SELECT * FROM EMPLOYEE1;

